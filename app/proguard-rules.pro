# Add project-specific ProGuard rules here.
# R8 is not enabled for release yet (isMinifyEnabled = false); revisit in the Phase 6
# performance pass. kotlinx.serialization keep rules will go here once the theme
# generation client (Phase 5) ships models over the wire.
